script(id="device-list-window-item-template", type="text/template")
  .hit-icon.device-widget(data-widget-type="window", data-hit-icon-type="devices.windowClose")
    span <%= name %>
    .widget-opts
      ul.device-btn-group.clearfix
        li.device-btn
          a.open(href="#") OPEN

script(id="device-add-window-template", type="text/template")
  .modal-header
    h2 Add A New <%= name %>
  .modal-body
    form.form-horizontal
      .control-group
        label(for="name").control-label Name
        .controls
          input(id="name", type="text", placeHolder="Bathroom Window")
      .control-group
        label(for="read_window").control-label Read Address
        .controls
          input(id="read_window", type="text", placeHolder="#/#/#")
  .modal-footer
    a(href="#").cancel.btn Cancel
    a(href="#").add.btn.btn-primary Add

script(id="device-view-window-template", type="text/template")
  .modal-header
    h2 <%= name %>
  .modal-body
    .row
      .span2
        label(for="name").pull-right Name
      .span3
        <%= name %>
    .row
      .span2
        label(for="read_window").pull-right Read Address
      .span3
        <%= getAddress('read_window').address %>
  .modal-footer
    a(href="#").delete.btn.pull-left Delete
    a(href="#").cancel.btn.btn-primary Close
