<div class="modal-header">
    <a class="close" href="#">x</a>
    <h2>{{ label }}</h2>
</div>

<div class="modal-body">
    <div class="row">
        <img src="{{ image thumbnail }}" class="cover pull-left">
        <ul>
            <li><strong>Genre</strong>: {{ genre }}</li>
            <li><strong>Description</strong>: {{ description }}</li>
        </ul>
    </div>
    <div class="row">
        <img src="{{ image fanart }}" class="cover pull-left">
    </div>
</div>

<div class="modal-footer">
    <a class="btn albums" href="#">Albums</a>
</div>