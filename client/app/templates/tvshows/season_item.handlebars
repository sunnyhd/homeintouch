<div class="seasonContainer">
    {{#if thumbnail}}
        <img src="{{ thumbnail }}">
    {{else}}
    	<img src="img/svg/media/defaultMovie.svg">
    {{/if}}

    <div class="season-caption">
    	<span>{{ label }}</span>
    </div>
</div>