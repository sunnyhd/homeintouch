{{ item.label }}
<br>
{{#if hasSpan}}
    {{ currentTime }} / {{ totalTime }}
    <br>
{{/if}}

<a class="stop" href="#">Stop</a>
{{#if playing}}
    <a class="pause" href="#">Pause</a>
{{else}}
    <a class="pause" href="#">Play</a>
{{/if}}
<br>

{{#if thumbnail}}
  <img src="{{ thumbnail }}">
{{/if}}